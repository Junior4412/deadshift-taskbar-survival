const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const L=require('../src/loot-system.js');const R=require('../src/recommendation-system.js');
test('cubo pode gerar equipamentos próprios para Maya e Bruno',()=>{let maya=L.generate({profile:'maya',rng:()=>.5}),bruno=L.generate({profile:'bruno',rng:()=>.5});assert.equal(maya.archetypeLabel,'MAYA');assert.equal(bruno.archetypeLabel,'BRUNO');assert.equal(R.category(maya),'dps');assert.equal(R.category(bruno),'tank')});
test('interface possui seleção de perfil, resultado e equipamento rápido',()=>{let game=fs.readFileSync(path.join(__dirname,'..','src','game.js'),'utf8');assert.match(game,/data-cube-profile/);assert.match(game,/cube-result/);assert.match(game,/data-quick-equip/);assert.match(game,/data-quick-best-all/)});
